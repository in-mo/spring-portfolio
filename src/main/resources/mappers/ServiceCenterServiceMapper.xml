<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.portfolio.mapper.ServiceCenterMapper">

	<insert id="addContent">
		insert into airbnb_custom_service (id, subject, content, readcount, reg_date)  
		values(#{id}, #{subject}, #{content}, #{readcount}, now()) 
	</insert>
	
	<select id="getContents" resultType="serviceCenterVo">
		select * 
		FROM airbnb_custom_service 
		ORDER BY num DESC 
		LIMIT #{startRow}, 9
	</select>
	
	<select id="getCountAll" resultType="int">
		select COUNT(*) FROM airbnb_custom_service
	</select>
	
	<select id="getContentByNum" resultType="serviceCenterVo">
		select * from airbnb_custom_service where num = #{ num }
	</select>
	
	<update id="updateReadcount">
		update airbnb_custom_service 
			set readcount = readcount + 1
			where num = #{ num }
	</update>
	
	<update id="updateContent">
		update airbnb_custom_service 
			set id = #{id},
			subject = #{subject},
			content = #{content},
			readcount = #{readcount}
			where num = #{num}
	</update>
	
	<delete id="deleteContent">
		delete from airbnb_custom_service where num = #{num}
	</delete>
</mapper>